# ==================================
# Graylog Environment Configuration
# ==================================

# GRAYLOG_PASSWORD_SECRET
# Secret used to encrypt user passwords.
# Must be at least 16 characters long.
# Generate using:
#   pwgen -N 1 -s 96
# or:
#   openssl rand -base64 64 | tr -d '\n'
GRAYLOG_PASSWORD_SECRET=

# GRAYLOG_ROOT_PASSWORD_SHA2
# SHA256 hash of the admin password.
# Generate using:
#   echo -n "yourpassword" | sha256sum
GRAYLOG_ROOT_PASSWORD_SHA2=

# GRAYLOG_ROOT_USERNAME
# Admin username (default: admin)
# Uncomment to override default
# GRAYLOG_ROOT_USERNAME=admin

# Graylog server timezone
# Example: Asia/Ho_Chi_Minh
GRAYLOG_ROOT_TIMEZONE=Asia/Ho_Chi_Minh

# Number of parallel message processors
GRAYLOG_PROCESSBUFFER_PROCESSORS=2
GRAYLOG_OUTPUTBUFFER_PROCESSORS=2